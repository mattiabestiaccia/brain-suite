---
phase: 03-dimension-exploration
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - commands/brain/product.md
  - commands/brain/tech.md
  - commands/brain/market.md
  - commands/brain/business.md
  - commands/brain/competitors.md
  - commands/brain/users.md
autonomous: false
requirements:
  - DIM-03

must_haves:
  truths:
    - "User runs /brain:product and gets the same exploration experience as /brain:explore product"
    - "User runs /brain:tech and gets the same exploration experience as /brain:explore tech"
    - "All 6 shortcut commands delegate to explore.md behavior"
    - "Shortcut commands do not duplicate explore logic"
  artifacts:
    - path: "commands/brain/product.md"
      provides: "Shortcut delegation to explore product"
      min_lines: 8
      contains: "product"
    - path: "commands/brain/tech.md"
      provides: "Shortcut delegation to explore tech"
      min_lines: 8
      contains: "tech"
    - path: "commands/brain/market.md"
      provides: "Shortcut delegation to explore market"
      min_lines: 8
      contains: "market"
    - path: "commands/brain/business.md"
      provides: "Shortcut delegation to explore business"
      min_lines: 8
      contains: "business"
    - path: "commands/brain/competitors.md"
      provides: "Shortcut delegation to explore competitors"
      min_lines: 8
      contains: "competitors"
    - path: "commands/brain/users.md"
      provides: "Shortcut delegation to explore users"
      min_lines: 8
      contains: "users"
  key_links:
    - from: "commands/brain/product.md"
      to: "commands/brain/explore.md"
      via: "Read tool delegation"
      pattern: "explore\\.md"
    - from: "commands/brain/tech.md"
      to: "commands/brain/explore.md"
      via: "Read tool delegation"
      pattern: "explore\\.md"
---

<objective>

Write 6 shortcut commands (`/brain:product`, `/brain:tech`, `/brain:market`, `/brain:business`, `/brain:competitors`, `/brain:users`) that delegate to `/brain:explore` behavior. Each shortcut sets the dimension name and instructs Claude to Read and follow the explore command file.

Purpose: Users can launch dimension exploration with a single command (DIM-03) instead of typing `/brain:explore <dimension>`.
Output: 6 shortcut command files replacing current stubs, plus user verification.

</objective>

<execution_context>
@/home/brusc/.claude/get-shit-done/workflows/execute-plan.md
@/home/brusc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-dimension-exploration/03-CONTEXT.md
@.planning/phases/03-dimension-exploration/03-RESEARCH.md
@.planning/phases/03-dimension-exploration/03-01-SUMMARY.md

# The file shortcuts delegate to
@commands/brain/explore.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 6 shortcut commands delegating to explore.md</name>
  <files>
commands/brain/product.md
commands/brain/tech.md
commands/brain/market.md
commands/brain/business.md
commands/brain/competitors.md
commands/brain/users.md
  </files>
  <action>
Replace each shortcut stub with a thin delegation wrapper (~15-20 lines each). All 6 follow the identical structure, differing only in the dimension name.

**Template for each shortcut (use research-recommended "most robust variant"):**

```markdown
# /brain:<dimension>

Explore the **<dimension>** dimension. This is a shortcut for `/brain:explore <dimension>`.

## Execution

1. Resolve the explore command path:
   ```bash
   EXPLORE_CMD=$(echo $HOME/.claude/commands/brain/explore.md)
   ```

2. Read the explore command file:
   Read `$EXPLORE_CMD` for the complete exploration instructions.

3. Execute ALL instructions from the explore command with dimension set to **<dimension>**.

The dimension is `<dimension>`. Do NOT ask the user which dimension to explore --
it is already determined. Proceed directly with the exploration flow.
```

Replace `<dimension>` with the appropriate name for each file:
- `product.md` -> dimension = **product**
- `tech.md` -> dimension = **tech**
- `market.md` -> dimension = **market**
- `business.md` -> dimension = **business**
- `competitors.md` -> dimension = **competitors**
- `users.md` -> dimension = **users**

**Critical constraints:**
- Each file MUST be under 25 lines (thin wrapper, no logic duplication)
- Each file MUST explicitly instruct Claude to Read the explore.md file (not just reference it abstractly)
- Each file MUST set the dimension name so Claude does NOT ask the user
- Do NOT copy any exploration logic from explore.md into shortcuts
- Use runtime path resolution via Bash `echo $HOME` (same pattern as explore.md and new.md -- symlink compatible)
  </action>
  <verify>
1. For each file, verify it exists and is short: `wc -l commands/brain/product.md commands/brain/tech.md commands/brain/market.md commands/brain/business.md commands/brain/competitors.md commands/brain/users.md` -- each should be 10-25 lines
2. Verify delegation pattern in all files: `grep -l 'explore.md' commands/brain/product.md commands/brain/tech.md commands/brain/market.md commands/brain/business.md commands/brain/competitors.md commands/brain/users.md` -- should return all 6 files
3. Verify each file sets its dimension name: `grep -l 'product' commands/brain/product.md`, `grep -l 'tech' commands/brain/tech.md`, etc. -- each should match
4. Verify no file contains exploration logic: `grep -c 'IDEA.md\|SESSION.md\|dimensions/' commands/brain/product.md` -- should be 0 (delegation only, no logic)
5. Verify path resolution: `grep -c 'echo.*HOME' commands/brain/product.md` -- should be >= 1
  </verify>
  <done>
All 6 shortcut commands exist as thin wrappers (10-25 lines each), each setting its dimension name and delegating to explore.md via Read tool with resolved path. No exploration logic is duplicated. Each file references explore.md explicitly.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify shortcut commands and end-to-end readiness</name>
  <files>
commands/brain/product.md
commands/brain/tech.md
commands/brain/market.md
commands/brain/business.md
commands/brain/competitors.md
commands/brain/users.md
  </files>
  <action>
Human verification of the 6 shortcut commands. The executor ensures install.sh has been run (so updated shortcuts are symlinked), then pauses for user to review.

Before pausing, the executor should:
1. Run `./install.sh` from the brain-suite repo to update symlinks
2. Confirm symlinks point to updated files: `ls -la ~/.claude/commands/brain/{product,tech,market,business,competitors,users}.md`
3. Present the verification checklist to the user
  </action>
  <verify>
User confirms the following:
1. Read one shortcut file (e.g., `commands/brain/product.md`) and verify:
   - It resolves the explore command path via Bash echo $HOME
   - It instructs Claude to Read explore.md
   - It sets the dimension to "product"
   - It tells Claude NOT to ask which dimension
   - It does NOT contain any exploration logic (no IDEA.md references, no template loading, no artifact generation)
2. Spot-check 1-2 other shortcuts to confirm identical structure with different dimension name
3. Verify all 6 files are present: `ls -la commands/brain/{product,tech,market,business,competitors,users}.md`
4. (Optional) Test one shortcut by running `/brain:product` in a project with existing `.brainstorm/IDEA.md`
  </verify>
  <done>User has reviewed shortcut commands and confirmed: all 6 exist as thin wrappers, each delegates to explore.md, no logic duplication, dimension names correct. Combined with Plan 01, all 10 Phase 3 requirements are covered.</done>
</task>

</tasks>

<verification>

Phase verification for this plan:
1. All 6 shortcut commands exist and are thin wrappers (10-25 lines)
2. Each shortcut delegates to explore.md via Read tool pattern
3. No exploration logic is duplicated across files
4. Combined with Plan 01, all 10 Phase 3 requirements are covered

</verification>

<success_criteria>

- 6 shortcut command files exist, each under 25 lines
- Each shortcut references explore.md and sets its dimension name
- No logic duplication across shortcuts or between shortcuts and explore.md
- User has verified the shortcut pattern via checkpoint
- DIM-03 (shortcut commands) requirement is fully satisfied

</success_criteria>

<output>
After completion, create `.planning/phases/03-dimension-exploration/03-02-SUMMARY.md`
</output>
