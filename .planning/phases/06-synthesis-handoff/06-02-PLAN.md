---
phase: 06-synthesis-handoff
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - commands/brain/analyze.md
  - commands/brain/synthesize.md
  - commands/brain/handoff.md
  - commands/brain/new.md
  - commands/brain/status.md
autonomous: true
requirements: [SYNTH-01, SYNTH-03, SYNTH-04, SYNTH-05]

must_haves:
  truths:
    - "User can run /brain:analyze with 2+ explored dimensions and get ANALYSIS.md"
    - "User can run /brain:synthesize with ANALYSIS.md present and get SYNTHESIS.md"
    - "User can run /brain:handoff with 2+ explored dimensions and get HANDOFF.md (with or without SYNTHESIS.md)"
    - "Each command validates prerequisites and shows a clear error if not met"
    - "Each command updates SESSION.md with a session note after generation"
    - "/brain:new archives ANALYSIS.md when starting fresh"
    - "/brain:status suggests /brain:analyze (not /brain:synthesize) when all dimensions explored"
  artifacts:
    - path: "commands/brain/analyze.md"
      provides: "/brain:analyze command"
      contains: "brain:analyze"
    - path: "commands/brain/synthesize.md"
      provides: "/brain:synthesize command"
      contains: "ANALYSIS.md"
    - path: "commands/brain/handoff.md"
      provides: "/brain:handoff command"
      contains: "HANDOFF.md"
  key_links:
    - from: "commands/brain/analyze.md"
      to: "agents/brain-synthesizer.md"
      via: "Invokes agent in analyze mode"
      pattern: "analyze.*mode"
    - from: "commands/brain/synthesize.md"
      to: "agents/brain-synthesizer.md"
      via: "Invokes agent in synthesize mode"
      pattern: "synthesize.*mode"
    - from: "commands/brain/handoff.md"
      to: "agents/brain-synthesizer.md"
      via: "Invokes agent in handoff mode"
      pattern: "handoff.*mode"
    - from: "commands/brain/analyze.md"
      to: ".brainstorm/SESSION.md"
      via: "Updates session notes after generation"
      pattern: "SESSION\\.md"
    - from: "commands/brain/new.md"
      to: ".brainstorm/ANALYSIS.md"
      via: "Archives ANALYSIS.md on fresh start"
      pattern: "ANALYSIS\\.md"
---

<objective>
Create the three command files (/brain:analyze, /brain:synthesize, /brain:handoff) and fix two existing commands (new.md, status.md) to complete the synthesis-handoff pipeline.

Purpose: Commands are thin orchestrators — they validate prerequisites, load context, invoke the brain-synthesizer agent in the correct mode, update SESSION.md, and show result summaries. All intelligence lives in the agent spec (Plan 01).

Output: 3 new/replaced command files + 2 updated existing command files.
</objective>

<execution_context>
@/home/brusc/.claude/get-shit-done/workflows/execute-plan.md
@/home/brusc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-synthesis-handoff/06-CONTEXT.md
@.planning/phases/06-synthesis-handoff/06-RESEARCH.md

# Agent spec created in Plan 01
@agents/brain-synthesizer.md
# Command patterns from existing commands
@commands/brain/new.md
@commands/brain/status.md
@commands/brain/explore.md

# Plan 01 summary for implementation details
@.planning/phases/06-synthesis-handoff/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /brain:analyze, /brain:synthesize, /brain:handoff command files</name>
  <files>commands/brain/analyze.md, commands/brain/synthesize.md, commands/brain/handoff.md</files>
  <action>
Create three thin command files following the established command pattern (header, setup, steps). Replace the existing stubs for synthesize.md and handoff.md. Create analyze.md as a new file.

All three commands share the same structure (Non-Interactive Command pattern from research):

```
# /brain:[command]
[one-line description]
---
## Setup
1. Validate prerequisites
2. Load all context
## Generate
3. Execute brain-synthesizer in [mode] mode
4. Update SESSION.md
5. Show result summary + suggest next command
```

**For each command:**

**analyze.md:**
- Description: Generate cross-dimensional analysis from explored dimensions. Produces ANALYSIS.md.
- Prerequisites: .brainstorm/SESSION.md exists (error + STOP if not), .brainstorm/IDEA.md exists (error + STOP if not), Glob .brainstorm/dimensions/*.md — count files — if < 2: "Servono almeno 2 dimensioni esplorate per l'analisi. Usa `/brain:explore` per esplorarne altre." + STOP.
- Context loading: Read IDEA.md, Read SESSION.md, Read ALL dimension files from Glob.
- Mode: analyze
- SESSION.md update: Add "Analisi cross-dimensionale generata" to Session Notes with date
- Next suggestion: "Ora puoi lanciare `/brain:synthesize` per generare la sintesi narrativa."

**synthesize.md:**
- Description: Generate narrative synthesis from cross-dimensional analysis. Produces SYNTHESIS.md.
- Prerequisites: .brainstorm/SESSION.md exists (error + STOP), .brainstorm/IDEA.md exists (error + STOP), .brainstorm/ANALYSIS.md exists — if missing: "Lancia prima `/brain:analyze` per generare l'analisi cross-dimensionale." + STOP.
- Context loading: Read IDEA.md, Read SESSION.md, Read ALL dimension files, Read ANALYSIS.md.
- Mode: synthesize
- SESSION.md update: Add "Sintesi narrativa generata" to Session Notes with date
- Next suggestion: "Ora puoi lanciare `/brain:handoff` per generare il documento di handoff per GSD."

**handoff.md:**
- Description: Generate GSD-ready handoff document from brainstorming artifacts. Produces HANDOFF.md.
- Prerequisites: .brainstorm/SESSION.md exists (error + STOP), .brainstorm/IDEA.md exists (error + STOP), Glob .brainstorm/dimensions/*.md — count files — if < 2: "Servono almeno 2 dimensioni esplorate per il handoff. Usa `/brain:explore` per esplorarne altre." + STOP.
- Context loading: Read IDEA.md, Read SESSION.md, Read ALL dimension files, Read SYNTHESIS.md if exists (for richer output), else Read ANALYSIS.md if exists.
- Mode: handoff
- SESSION.md update: Add "Documento di handoff generato" to Session Notes with date. Update Status field from current value to "handoff-complete".
- Next suggestion: "HANDOFF.md è pronto! Puoi usarlo come input per `/gsd:new-project` per avviare la pianificazione."
- No suggestion of further brain: commands — handoff is the terminal step.

**Important patterns to follow (from existing commands):**
- Reference path resolution via Bash: `BRAIN_REF=$(echo $HOME/.claude/brain-suite/references)` — NOT needed here since these commands don't load reference files, but validate .brainstorm/ paths
- Error messages in Italian (matching existing status.md pattern)
- Each command is fully self-contained — no Skill tool delegation between these commands
- The agent behavior is invoked by stating "Execute brain-synthesizer behavior in [mode] mode" — the command file IS the prompt that triggers agent behavior
  </action>
  <verify>
Read all three command files and verify:
1. analyze.md: validates 2+ dimensions, does NOT require ANALYSIS.md
2. synthesize.md: requires ANALYSIS.md to exist
3. handoff.md: validates 2+ dimensions, works with or without SYNTHESIS.md
4. All three update SESSION.md after generation
5. Error messages are in Italian
6. Each file references the brain-synthesizer agent mode
  </verify>
  <done>
Three command files exist with correct prerequisite validation, context loading, agent mode invocation, SESSION.md updates, and next-step suggestions. Each command is a complete, self-contained prompt.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix new.md archive step and status.md suggestion</name>
  <files>commands/brain/new.md, commands/brain/status.md</files>
  <action>
Two small fixes to existing command files:

**new.md — Add ANALYSIS.md archival:**
In the "Existing Session Check" section, in the "If start fresh" block, the archive bash commands (lines 42-47) move IDEA.md, SESSION.md, dimensions/, sessions/, SYNTHESIS.md, HANDOFF.md. Add ANALYSIS.md to this list.

Add this line after the HANDOFF.md line (line 47):
```bash
[ -f .brainstorm/ANALYSIS.md ] && mv .brainstorm/ANALYSIS.md "$ARCHIVE_DIR/"
```

This ensures starting a fresh session archives the analysis document along with everything else.

**status.md — Update all-explored suggestion:**
In the "Case 3: All dimensions explored" section (around line 113-115), the current text suggests `/brain:synthesize`. Change it to suggest `/brain:analyze` since that is now the entry point of the chain.

Replace:
```
Tutte le dimensioni sono state esplorate! Puoi lanciare `/brain:synthesize` per generare una sintesi completa.
```

With:
```
Tutte le dimensioni sono state esplorate! Puoi lanciare `/brain:analyze` per generare l'analisi cross-dimensionale.
```
  </action>
  <verify>
1. Read commands/brain/new.md — verify ANALYSIS.md appears in the archive bash block alongside SYNTHESIS.md and HANDOFF.md
2. Read commands/brain/status.md — verify Case 3 mentions `/brain:analyze` not `/brain:synthesize`
  </verify>
  <done>
new.md archives ANALYSIS.md when starting fresh. status.md suggests /brain:analyze as the entry point when all dimensions are explored.
  </done>
</task>

</tasks>

<verification>
- commands/brain/analyze.md exists with prerequisite validation for 2+ dimensions
- commands/brain/synthesize.md exists with prerequisite validation for ANALYSIS.md
- commands/brain/handoff.md exists with prerequisite validation for 2+ dimensions and graceful SYNTHESIS.md fallback
- commands/brain/new.md includes ANALYSIS.md in archive block
- commands/brain/status.md suggests /brain:analyze when all dimensions explored
- All three new commands update SESSION.md after generation
- Error messages are in Italian
</verification>

<success_criteria>
The complete analyze -> synthesize -> handoff pipeline is operational. Users can run each command in sequence, each validates its prerequisites, generates its document via brain-synthesizer, and suggests the next step. The existing commands (new.md, status.md) are updated to reference the correct entry point (/brain:analyze).
</success_criteria>

<output>
After completion, create `.planning/phases/06-synthesis-handoff/06-02-SUMMARY.md`
</output>
