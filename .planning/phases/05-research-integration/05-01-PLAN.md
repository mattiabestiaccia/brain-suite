---
phase: 05-research-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/brain-researcher.md
autonomous: true
requirements:
  - AGT-02
  - RES-03

must_haves:
  truths:
    - "brain-researcher agent spec defines a non-conversational background worker that fetches factual data via Exa MCP"
    - "Researcher produces 3-5 structured bullet points with source name, year, and URL"
    - "Researcher writes results to .brainstorm/.research-pending/research-<timestamp>.md"
    - "Researcher handles errors gracefully (Exa unavailable, no results, partial data)"
  artifacts:
    - path: "agents/brain-researcher.md"
      provides: "Complete brain-researcher agent specification"
      contains: "mcp__exa__web_search_exa"
  key_links:
    - from: "agents/brain-researcher.md"
      to: "Exa MCP tools"
      via: "tool declarations in YAML frontmatter"
      pattern: "mcp__exa__web_search_exa"
    - from: "agents/brain-researcher.md"
      to: ".brainstorm/.research-pending/"
      via: "output file write instructions"
      pattern: "research-pending"
---

<objective>

Create the complete brain-researcher agent specification, replacing the Phase 1 stub with a full behavioral prompt. The researcher is a non-conversational background worker spawned by the explorer during dimension exploration. It receives a research question, formulates 2-3 Exa MCP queries, executes them, synthesizes results into 3-5 bullet points with sources, and writes a structured result file.

Purpose: The researcher agent must exist as a complete spec before the explorer can be modified to spawn it (Plan 02). This establishes the contract: what the researcher expects as input, what it produces as output, and where it writes results.

Output: `agents/brain-researcher.md` — complete agent spec (~80-120 lines)

</objective>

<execution_context>
@/home/brusc/.claude/get-shit-done/workflows/execute-plan.md
@/home/brusc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-research-integration/05-CONTEXT.md
@.planning/phases/05-research-integration/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write complete brain-researcher agent spec</name>
  <files>agents/brain-researcher.md</files>
  <action>
Replace the 12-line stub in agents/brain-researcher.md with the complete agent specification (~80-120 lines). The spec must define:

**YAML frontmatter:**
- name: brain-researcher
- description: Fetches factual data via Exa MCP to support dimension exploration
- tools: Read, Write, Bash, Glob, mcp__exa__web_search_exa, mcp__exa__get_code_context_exa

**Role section:**
- Non-conversational background agent. Spawned by explorer during dimension exploration.
- Receives a research question, fetches factual data, writes structured results, terminates.
- Does NOT interact with the user. Does NOT participate in conversation. Silent worker.

**Execution Flow section (numbered steps):**
1. Parse the research question from the task prompt
2. Determine claim type (market size, competitor, tech feasibility, user behavior, pricing)
3. Formulate 2-3 search queries from different angles (use the query formulation table from RESEARCH.md)
4. Execute queries via Exa MCP tools:
   - `mcp__exa__web_search_exa` for market, competitor, user, pricing claims
   - `mcp__exa__get_code_context_exa` for tech feasibility claims
   - Request numResults: 10 per query, prefer recent sources (2024-2026)
5. Synthesize top findings into 3-5 bullet points, each with: key data point + Source: [Name] ([Year]) [URL]
6. Create output directory: `mkdir -p .brainstorm/.research-pending`
7. Write results to `.brainstorm/.research-pending/research-<timestamp>.md` using the result file format below
8. Complete (subagent terminates)

**Query Formulation table:**

| Claim Type | Query Strategy | Exa Tool |
|------------|---------------|----------|
| Market size ("il mercato è grande") | "[domain] market size 2025 2026 revenue" + "[domain] market growth forecast" | web_search_exa |
| Competitor claims ("ci sono pochi competitor") | "[domain] competitors landscape" + "alternatives to [product type]" | web_search_exa |
| Tech feasibility ("dovrebbe essere fattibile") | "[technology] implementation guide" + "[technology] limitations gotchas" | get_code_context_exa |
| User behavior ("gli utenti vogliono X") | "[domain] user research survey 2025" + "[user type] pain points needs" | web_search_exa |
| Pricing ("il prezzo giusto è X") | "[domain] pricing benchmarks" + "[product type] pricing models 2025" | web_search_exa |

**Result File Format section:**

```markdown
# Research Results

**Query:** [original research question from explorer]
**Searched:** [YYYY-MM-DD HH:MM]
**Status:** [found_data | no_relevant_data | partial_data | error]

## Findings

- **[Key data point]** — Source: [Name] ([Year]) [URL]
- **[Key data point]** — Source: [Name] ([Year]) [URL]
- **[Key data point]** — Source: [Name] ([Year]) [URL]

## Raw Queries Used

1. [query 1] → [N results]
2. [query 2] → [N results]

---
*Research by brain-researcher for /brain:explore on [date]*
```

**Error Handling section:**
- If Exa MCP tools are unavailable: write result file with status "error" and message "Exa MCP tools not available in this session"
- If no relevant results found: write status "no_relevant_data" with empty Findings
- If partial results (some queries fail, some succeed): write status "partial_data" with available findings
- ALWAYS write a result file, even on failure. The explorer expects it.

**Constraints section:**
- Maximum 3 queries per research request
- Maximum 10 results per query (numResults: 10)
- Prefer recent sources (2024-2026)
- Always include source URL for verifiability
- Keep bullet points concise (1-2 sentences each)
- Do NOT interpret or editorialize -- report data, let the explorer analyze
- Do NOT interact with the user in any way
- Do NOT write to any file outside .brainstorm/.research-pending/

**Exa MCP Best Practices section (brief):**
- Use natural language queries, not keyword soup
- Use `category` parameter ("company", "research paper") when appropriate for market/competitor claims
- For tech queries, use `get_code_context_exa` which searches GitHub, Stack Overflow, and docs
- Keep queries concise and specific -- Exa works best with focused queries

The agent spec must follow the same markdown-as-prompt philosophy as brain-explorer.md: the file IS the prompt, clear and actionable, a different Claude instance can execute it without asking questions.
  </action>
  <verify>
Verify the file:
1. `wc -l agents/brain-researcher.md` -- should be 80-120 lines
2. `head -6 agents/brain-researcher.md` -- verify YAML frontmatter has correct tools including Exa MCP
3. `grep -c "mcp__exa__web_search_exa" agents/brain-researcher.md` -- should appear at least 2 times (frontmatter + query table)
4. `grep -c "research-pending" agents/brain-researcher.md` -- should appear at least 2 times (execution flow + result format)
5. `grep -c "Status:" agents/brain-researcher.md` -- should reference all 4 status types (found_data, no_relevant_data, partial_data, error)
  </verify>
  <done>agents/brain-researcher.md is a complete agent spec (80-120 lines) that defines a non-conversational background worker with Exa MCP query patterns, structured output format, error handling, and clear constraints. The stub placeholder text is gone.</done>
</task>

</tasks>

<verification>
- agents/brain-researcher.md exists and is 80-120 lines (not the 12-line stub)
- YAML frontmatter declares Exa MCP tools
- Execution flow is numbered and specific
- Query formulation table maps claim types to query strategies
- Result file format matches the contract the explorer will expect
- Error handling covers all 4 status types
- Constraints section prevents scope creep
</verification>

<success_criteria>
brain-researcher.md is a complete, actionable agent spec that a Claude instance can execute as a background Task to fetch factual data and produce structured results. The file replaces the Phase 1 stub entirely.
</success_criteria>

<output>
After completion, create `.planning/phases/05-research-integration/05-01-SUMMARY.md`
</output>
