---
phase: 04-session-management
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/brain/explore.md
autonomous: true
requirements:
  - SESS-03
  - DIM-05

must_haves:
  truths:
    - "When user explores an already-explored dimension, they are asked upfront: deepen or restart"
    - "Deepen loads the previous dimension file into context and resumes as continuation, steering naturally toward weak spots without listing them"
    - "Restart archives the previous file to .brainstorm/dimensions/archive/ with timestamp before starting fresh"
    - "Deepening produces an updated file that replaces the previous one (single source of truth)"
    - "Custom dimensions registered in SESSION.md are accepted as valid by explore.md"
    - "Custom dimension templates are loaded from .brainstorm/templates/ not from reference templates"
    - "At closure, explorer gives a more systematic next-dimension suggestion based on SESSION.md state + dimensions-guide.md relationships + conversation content"
  artifacts:
    - path: "commands/brain/explore.md"
      provides: "Modified exploration command with re-exploration flow, custom dimension support, and enhanced next-dimension suggestion"
      min_lines: 350
  key_links:
    - from: "commands/brain/explore.md"
      to: ".brainstorm/dimensions/archive/"
      via: "Bash mv command for restart archival"
      pattern: "dimensions/archive"
    - from: "commands/brain/explore.md"
      to: ".brainstorm/SESSION.md"
      via: "Read to check custom dimension registration"
      pattern: "SESSION\\.md.*custom"
    - from: "commands/brain/explore.md"
      to: ".brainstorm/templates/"
      via: "Read custom dimension template"
      pattern: "\\.brainstorm/templates/"
    - from: "commands/brain/explore.md"
      to: "dimensions-guide.md"
      via: "Read at closure for next-dimension suggestion"
      pattern: "dimensions-guide"
---

<objective>
Modify /brain:explore to support re-exploration (deepen/restart flow), custom dimension validation, and enhanced proactive next-dimension suggestion.

Purpose: Users can revisit already-explored dimensions with meaningful choices (deepen existing work or start fresh with archive), explore custom dimensions registered via /brain:add-dimension, and get better guidance on what to explore next.
Output: Updated explore.md with three new capabilities integrated into the existing exploration flow.
</objective>

<execution_context>
@/home/brusc/.claude/get-shit-done/workflows/execute-plan.md
@/home/brusc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-session-management/04-CONTEXT.md
@.planning/phases/04-session-management/04-RESEARCH.md
@.planning/phases/03-dimension-exploration/03-01-SUMMARY.md
@commands/brain/explore.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Modify explore.md with re-exploration, custom dimensions, and enhanced suggestions</name>
  <files>commands/brain/explore.md</files>
  <action>
Read the current explore.md (312 lines) and modify it with three targeted changes. Preserve ALL existing content and structure -- these are surgical additions, not a rewrite.

**Change 1: Extend dimension validation (Step 2) to accept custom dimensions**

Replace the current Step 2 validation with two-step validation:

```markdown
2. **Validate the dimension:**
   - Extract dimension from `$ARGUMENTS` (first word or full argument, lowercase).
   - Built-in dimensions: product, tech, market, business, competitors, users.
   - If the dimension matches a built-in: load template from `$BRAIN_TPL/<dimension>.md` in Step 3.
   - If NOT built-in: read `.brainstorm/SESSION.md` and check the Explored Dimensions table for a row with a matching dimension slug.
     - If found in SESSION.md: this is a registered custom dimension. In Step 3, load template from `.brainstorm/templates/<slug>.md` instead of `$BRAIN_TPL/`.
     - If NOT found in either: list ALL available dimensions (the 6 built-ins + any custom dimensions from SESSION.md) and STOP.
   - Set an internal flag `IS_CUSTOM` to track whether this is a custom or built-in dimension (affects template loading path in Step 3).
```

Update Step 3 (Load context files) to use the correct template path based on IS_CUSTOM:
- Built-in: `$BRAIN_TPL/<dimension>.md`
- Custom: `.brainstorm/templates/<slug>.md`
- If custom template file is missing, tell user to run `/brain:add-dimension` again and STOP.

**Change 2: Replace Step 6 with full re-exploration flow (deepen/restart)**

Replace the current Step 6 (which is a brief note about Phase 4) with the complete re-exploration flow:

```markdown
6. **Check for previous exploration:**
   - If `.brainstorm/dimensions/<dimension>.md` already exists:
     - Ask the user directly: "Hai gia esplorato **[dimension]**. Vuoi approfondire quello che avevi fatto, o ricominciare da zero?"
     - **If user chooses to deepen:**
       - Read the existing dimension file into context
       - Note internally which sections have substantive content vs placeholders/thin content
       - Proceed to the Opening, but adapt: acknowledge previous exploration briefly ("Riprendiamo da dove eravamo rimasti")
       - During conversation, naturally steer toward sections with placeholders or thin content through questions -- NEVER list missing sections, NEVER say "last time we didn't cover X" (same invisible tracking principle from Phase 3)
       - At closure: write an updated dimension file that incorporates BOTH the previous content (for untouched sections) and new conversation content (for discussed sections). The updated file REPLACES the previous one (single source of truth). A new session log is created.
     - **If user chooses to restart:**
       - Archive the existing dimension file:
         ```bash
         mkdir -p .brainstorm/dimensions/archive
         TIMESTAMP=$(date +%Y-%m-%d-%H%M)
         mv .brainstorm/dimensions/<dimension>.md .brainstorm/dimensions/archive/<dimension>-$TIMESTAMP.md
         ```
       - Proceed with standard fresh exploration (no change to rest of flow)
       - A new dimension file and session log are created from scratch
   - If dimension file does NOT exist: proceed with standard fresh exploration (no change)
```

**Change 3: Enhance the next-dimension suggestion at closure**

Find the existing closure/suggest-next section (Step 5 in Closure) and enhance it to be more systematic:

1. Load dimensions-guide.md at closure time (already done in current version)
2. Read SESSION.md to check which dimensions are explored vs not started
3. Prioritize the suggestion based on (in order):
   - Dimensions mentioned or hinted at during the just-completed conversation (strongest signal)
   - Dimension relationships from dimensions-guide.md (e.g., after product -> suggest users)
   - Simple gap filling (any unexplored dimension)
4. Include custom dimensions in the suggestion pool if they are unexplored
5. The suggestion must reference conversation content: "Ti suggerisco **X** perche durante [dimension] hai menzionato [topic] che tocca direttamente [X]."
6. If ALL dimensions are explored: congratulate and suggest `/brain:synthesize` (placeholder for Phase 6)

**Critical anti-patterns to avoid:**
- Do NOT restructure the entire file. Make targeted insertions/replacements.
- Do NOT reveal internal tracking in deepen mode (invisible coverage principle).
- Do NOT remove existing content that is still valid.
- Archive path is `.brainstorm/dimensions/archive/` (NOT `.brainstorm/archive/` which is for /brain:new session archives).
  </action>
  <verify>
Read the modified commands/brain/explore.md and verify:
1. Step 2 validates both built-in AND custom dimensions (two-step validation)
2. Step 3 loads template from correct path based on built-in vs custom
3. Step 6 has full deepen/restart flow (not the old brief note)
4. Deepen mode loads previous file, uses invisible tracking, produces updated file
5. Restart mode archives to .brainstorm/dimensions/archive/ with timestamp
6. Closure section has enhanced next-dimension suggestion with SESSION.md check
7. Custom dimensions included in suggestion pool
8. All existing content (opening, conversation, depth gating, artifact generation) preserved
9. File is at least 350 lines
  </verify>
  <done>
explore.md contains: (1) two-step dimension validation accepting custom dimensions from SESSION.md, (2) full deepen/restart re-exploration flow replacing the Phase 4 placeholder, (3) enhanced next-dimension suggestion that checks SESSION.md state and prioritizes based on conversation content + dimension relationships + gaps, and (4) all existing exploration flow preserved.
  </done>
</task>

</tasks>

<verification>
- explore.md accepts custom dimension names registered in SESSION.md
- Custom dimension templates loaded from .brainstorm/templates/, not from reference templates
- Re-exploration asks deepen/restart upfront, not at end
- Deepen mode uses invisible tracking (no section listing)
- Restart archives to .brainstorm/dimensions/archive/ (separate from /brain:new archives)
- Next-dimension suggestion is systematic: conversation signals > dimension relationships > gap filling
- Existing exploration flow (opening, conversation, depth gating, artifact generation, session log) fully preserved
</verification>

<success_criteria>
- commands/brain/explore.md is updated (350+ lines) with all three changes integrated
- Re-exploration flow works for both deepen and restart paths
- Custom dimensions from SESSION.md are accepted and templates loaded from correct path
- Next-dimension suggestion at closure is more systematic and gap-aware
- No regression in existing exploration behavior
</success_criteria>

<output>
After completion, create `.planning/phases/04-session-management/04-02-SUMMARY.md`
</output>
